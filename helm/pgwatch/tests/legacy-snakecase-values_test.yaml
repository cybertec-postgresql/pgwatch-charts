suite: legacy-snakecase-values
templates:
  - templates/NOTES.txt
values:
  - ../test_scenarios/template-checks/legacy-snakecase-values.yaml
tests:
  - it: NOTES.txt warns about legacy snake_case keys and no other legacy warning
    asserts:
      - matchRegex:
          path: raw
          pattern: "Detected legacy snake_case values"
      - notMatchRegex:
          path: raw
          pattern: "Detected legacy string boolean values"
      - notMatchRegex:
          path: raw
          pattern: "Detected deprecated inline external database credentials"
      - notMatchRegex:
          path: raw
          pattern: "Detected legacy string value for pgwatch.image"
